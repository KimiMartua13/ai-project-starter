// schema.dbml
//
// This file is generated/re-written by the DB Designer.
// Context sources: docs/architecture.md + AGENTS_BACKEND.md
// Defines tables, columns, primary/foreign keys, indexes, and relationships.

/*
// Example DBML Template & Syntax Guide:

Table users {
  id uuid [pk, default: `gen_random_uuid()`]
  name varchar(255) [not null]
  email varchar(255) [unique, not null]
  password varchar(255) [not null]
  created_at timestamp [not null, default: `now()`]
  updated_at timestamp [not null, default: `now()`]
  deleted_at timestamp [null]

  indexes {
    email [name: 'idx_users_email']
  }
}

Table posts {
  id uuid [pk, default: `gen_random_uuid()`]
  user_id uuid [not null]
  title varchar(255) [not null]
  content text
  created_at timestamp [not null, default: `now()`]
}

// Relationships
Ref: posts.user_id > users.id [delete: cascade]
*/
